# Vercel production findings

Checked `https://memecoin-radar-kappa.vercel.app/` on 2026-08-27. The frontend shell renders, but the discovery table remains in skeleton/loading state and never receives token data.

Direct request to `https://memecoin-radar-kappa.vercel.app/api/trpc/tokens.discover` returns Vercel `500 INTERNAL_SERVER_ERROR` with `FUNCTION_INVOCATION_FAILED` (`cle1::jnx6h-1787845057174-701111b8bfc3`). Browser console had no additional output. This confirms the mismatch is a production serverless API crash, not a CSS/rendering issue.

Next diagnostic target: Vercel function runtime/build logs and production environment variables, especially DATABASE_URL, BUILT_IN_FORGE_API_URL, BUILT_IN_FORGE_API_KEY, OAuth/runtime values, and compatibility of the tRPC function wrapper with the deployed build.

Additional Vercel inspection: the configured Vercel team `conco's projects` (`team_0VHYsdt0hEyiExlJmzY5mwCE`) reports `linkedProjects: []`; querying build logs for `memecoin-radar-kappa.vercel.app` under that team returns `404 Deployment not found`. This indicates the production URL belongs to a different Vercel account/team or is not linked to the configured team, so its private runtime logs/settings cannot be fetched from this session.

Second production check on 2026-08-27: the root now includes the Settings link, confirming a newer frontend deployment is live, but the valid batch request to `/api/trpc/tokens.discover` still returns `500 FUNCTION_INVOCATION_FAILED` (`cle1::w9bk2-1787846400368-c610cd10b4a7`). The dashboard therefore remains stuck in skeleton state. This rules out a frontend-only mismatch; the production serverless function is still crashing before returning tRPC JSON.

Third production check: `auth.me` also returns `FUNCTION_INVOCATION_FAILED` 500, confirming the failure affects the entire tRPC serverless function, not only the market-data procedure. The Vercel `_logs` link redirects to the Vercel login page, so the stack trace cannot be read without the owner's Vercel session.

Authenticated Vercel Runtime Logs identified the exact root cause: `Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/var/task/server/routers' imported from /var/task/api/trpc/[...path].js`. The deployed Node ESM function rejects extensionless relative imports emitted from the TypeScript source. This affects all tRPC procedures during module load, explaining the universal 500 response.

Final verification on 2026-08-27: `https://memecoin-radar-kappa.vercel.app/api/trpc/tokens.discover` now returns a successful tRPC JSON payload containing discovered token records and scoring fields. The public dashboard deployment is serving the updated UI and is no longer blocked by the serverless module-load failure. GitHub `conco1236/memecoin-radar` `main` points to commit `1070c31b79ef5831667352352a526093327a8310` with message `fix: resolve Vercel serverless ESM imports`.

Health panel UI verification on 2026-08-27: local preview rendered the new `DATA SOURCE HEALTH` panel in English and `SỨC KHỎE NGUỒN DỮ LIỆU` in Vietnamese. Browser extraction confirmed the panel appears above filters and remains structurally aligned with the dashboard; desktop and mobile screenshots show no panel overflow. During initial loading it correctly displays `CHECKING` / `ĐANG KIỂM TRA`, while source rows populate after the health query resolves.

Login failure diagnosis on 2026-08-27: the browser correctly redirects to `/api/oauth/callback`, but the Vercel `api/` surface had no OAuth callback function. `api/index.ts` only mounted tRPC, while OAuth was registered only in the long-running Express bootstrap, so production never executed state validation or session-cookie creation. Added `api/oauth/callback.ts` as a dedicated Vercel function that mounts the existing OAuth callback logic. Added callback validation tests; final verification passes with 31 Vitest tests, typecheck, and production build.

Post-fix production check on 2026-08-27: `https://memecoin-radar-kappa.vercel.app/api/oauth/callback` still returns Vercel 404 NOT_FOUND. This confirms the live URL has not yet been redeployed with checkpoint `45e9a52a`; the code-side fix is ready, but end-to-end production login verification cannot succeed until the owner publishes/redeploys that checkpoint.

Post-redeploy check on 2026-08-27: the live URL `https://memecoin-radar-kappa.vercel.app/api/oauth/callback` still returns `404 NOT_FOUND`. This is not a cookie or OAuth-state failure yet; Vercel is not serving the callback function at the live domain. The deployed project likely uses a different GitHub commit/root directory or the redeploy occurred on a different Vercel project/domain. The owner should verify the deployment commit includes `api/oauth/callback.ts` and the project Root Directory is the repository root (`.`).

Latest production check after GitHub API sync on 2026-08-27: `/api/oauth/callback` at `https://memecoin-radar-kappa.vercel.app` still returns `404 NOT_FOUND`. GitHub `main` now contains `api/oauth/callback.ts` and `api/scheduled/healthCheck.ts`, with main at commit `d38ed93f6698994e8eca51ac913f6dd53a48cc77`. Therefore the live Vercel domain is not deploying this GitHub repository/branch or is configured with a different Root Directory/project. This is confirmed as a Vercel project-link/deployment-source issue rather than an OAuth cookie issue.

Vercel connector audit on 2026-08-27: the authenticated team `conco's projects` (`team_0VHYsdt0hEyiExlJmzY5mwCE`) reports `linkedProjects: []` and `list_projects` returns no projects. GitHub main is at `d38ed93f6698994e8eca51ac913f6dd53a48cc77` and contains `api/oauth/callback.ts`, but the live domain still returns 404. The remaining login blocker is deployment linkage/ownership: the Vercel project serving `memecoin-radar-kappa.vercel.app` is not the Vercel project/repository currently connected to this session, or its Root Directory is not the repository root.

Deployment page audit for `CCtibY88r5Ks2rJv47WLcaXzJojT` on 2026-08-27: Vercel shows Status Ready, Environment Production, Source `main`, but the source commit is `1070c31 fix: resolve Vercel serverless ESM imports`. It lists only `memecoin-radar-git-main-concos-projects.vercel.app` and `memecoin-radar-j6vb6xzk9-concos-projects.vercel.app`. The OAuth callback fix is in a later GitHub main commit (`d38ed93f...`), so this deployment is an older production deployment and cannot contain `api/oauth/callback.ts`.

Deployment list audit on 2026-08-27: production alias `memecoin-radar-kappa.vercel.app` currently points to ready deployment `memecoin-radar-1aqlsthfq-concos-projects.vercel.app` at old commit `1070c31`. Newer GitHub commits created through per-file API sync (`d38ed93`, `43327f3`, `7cc2c74`, etc.) appear as separate Vercel production deployments, but most are Error because they are partial repository states. The correct fix is to publish the complete repository state as one coherent Git commit, then redeploy that commit—not to redeploy the old 1070c31 deployment.

Vercel deployment diagnosis on 2026-08-27: deployment `5HDDwZdUMXDS8MXBX6WCK7qK31Wu` for commit `56f46e2` was explicitly **Blocked** because commit email `3.2479659e+07+conco1236@users.noreply.github.com` could not be matched to the GitHub account. Fixed by creating commit `3585861a0778511a1ee0ebcb8470f568d2006f83` with verified primary email `concocon1236@gmail.com`. Vercel now shows a new Production deployment `memecoin-radar-qjp88mg8c-concos-projects.vercel.app` for `3585861`, status Building; the old kappa alias has not yet switched.

Production verification on 2026-08-27: after commit `3585861` with verified GitHub email, deployment `memecoin-radar-qjp88mg8c-concos-projects.vercel.app` finished serving the OAuth route. Browser access to both the temporary deployment URL and `https://memecoin-radar-kappa.vercel.app/api/oauth/callback` returns `{"error":"code and state are required"}` (HTTP 400), confirming the alias now serves the new callback instead of Vercel 404. Earlier curl received Vercel SSO 302 because the temporary deployment is protected; the authenticated browser session reached the application route successfully.

Login button diagnosis on 2026-08-27: production DOM shows the button is `disabled: false`, `display: block`, `pointer-events: auto`, and within the expected header bounds. The production bundle contains `new URL("/app-auth")` and `appId` set to an empty string, instead of an absolute OAuth portal URL and populated app ID. This proves `VITE_OAUTH_PORTAL_URL` and `VITE_APP_ID` were not injected into the deployed frontend build. Clicking therefore navigates nowhere useful or fails before OAuth starts. Vercel Environment Variables UI lists both variable names, but the deployed build was created before/without their production values being applied; a new deployment is required after correcting values and rebuilding.

Production OAuth env remediation on 2026-08-27: Vercel project `concos-projects/memecoin-radar` had `VITE_APP_ID` and `VITE_OAUTH_PORTAL_URL` rows, but the prior deployed bundle still contained `appId:""` and `new URL("/app-auth")`. With user confirmation, `VITE_APP_ID` was set to the project identifier from `.project-config.json`, `VITE_OAUTH_PORTAL_URL` was set to `https://manus.im`, and `OAUTH_SERVER_URL` was restored/confirmed as `https://api.manus.im` after a menu-selection ambiguity. Production redeploy `ENmHHtzQbR9e6ucsBTKyEZeeXpcs` is Ready and currently assigned to `memecoin-radar-kappa.vercel.app`; its source is main commit `3585861`. The next required check is cache-busted bundle inspection and a click test of the login button; if the asset hash remains old, redeploy with Build Cache disabled.
